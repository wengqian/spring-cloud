server:  port: 7776eureka:  instance:      hostname: localhost      namespace: registry-center  client:    service-url:      defaultZone: http://${eureka.instance.hostname}:7775/eureka/#   client:#      register-with-eureka: false #表示是否注册自身到eureka服务器#      fetch-registry: false#      service-url:#            defaultZone: http://${eureka.instance.hostname}:${server.port}/${eureka.instance.namespace}/spring:  application:    name: registry-center