server:  port: 7780eureka:  client:    service-url:      defaultZone: http://127.0.0.1:7777/eureka/spring:  application:    name: micro-service-feignclientfeign:  #okhttp#  okhttp:#   enabled: true  #线程池  httpclient:    enabled: true    max-connections: 200 # 默认值    max-connections-per-route: 50 # 默认值  hystrix:    enabled: true    command:      default:        execution:          timeout:            #是否开启超时，默认是true，开启。            enable: true          isolation:            #THREAD|SEMAPHORE            strategy: SEMAPHORE            semaphore:              #相当于亚毫秒级的，指任意时间点允许的并发数。当请求达到或超过该设置值后，其其余就会被拒绝。默认值是100              maxConcurrentRequests: 100            thread:              #设置thread和semaphore两种隔离策略的超时时间，默认值是1000。              timeoutInMilliseconds: 10000              #发生线程冲突的时候是否中断线程，默认是true              interruptOnTimeout: true              #取消时是否中断线程，默认是false              interruptOnCancel: false##Hystrix支持，如果为true，hystrix库必须在classpath中#feign.hystrix.enabled=false###请求和响应GZIP压缩支持#feign.compression.request.enabled=true#feign.compression.response.enabled=true##支持压缩的mime types#feign.compression.request.enabled=true#feign.compression.request.mime-types=text/xml,application/xml,application/json#feign.compression.request.min-request-size=2048### 日志支持#logging.level.project.user.UserClient: DEBUG